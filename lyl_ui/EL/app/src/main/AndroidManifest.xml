<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.el">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_head"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainpage" />

        <service android:name=".AudioService" />
        <service android:name=".MadiaService" />
        <service
            android:name=".FindEnemy"
            android:label="@string/app_name" />
        <activity android:name=".registerPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".creatorList"
            android:theme="@style/CreateListStyle" />
        <activity
            android:name=".goodsStorage"
            android:theme="@style/CreateListStyle" />
        <activity android:name=".gamePage" />
        <activity android:name=".gameWait"></activity><!-- ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>